version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: solid-service-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: solid
      POSTGRES_PASSWORD: solid123
      POSTGRES_DB: solid_service_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U solid']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: solid-service-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
