{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd packages/database && npx prisma generate && cd ../.. && npm run build"
  },
  "deploy": {
    "startCommand": "node apps/api/src/scripts/wait-for-db.js && cd packages/database && npx prisma migrate deploy && cd ../../apps/api && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
