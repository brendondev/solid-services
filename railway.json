{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules apps/api/dist && npm install && cd packages/database && npx prisma generate && cd ../.. && npm run build"
  },
  "deploy": {
    "startCommand": "cd packages/database && npx prisma migrate deploy && cd ../../apps/api && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
